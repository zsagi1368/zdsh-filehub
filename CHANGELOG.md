# Changelog

All notable changes to zDSH FileHub are documented here. Format follows
Keep a Changelog; versioning is SemVer.

## [0.1.0] - unreleased

### Added

- M0 scaffold: dual-face plugin skeleton (ESM host half + ModuleLoader-handshake
  client bundle), wire contract single source (`src/contract.ts`), build and CI
  gates, bilingual README, honest SECURITY.md.
- Clean-room development baseline derived from the FileHub research campaign
  (fork repo `PluginR&D/docs/filehub/R1-R5`, plan `P01` v2): upload channels,
  @ mention pipeline, document reading, vision captioning waterfall and a
  cross-session file console land milestone by milestone (M1-M5).
