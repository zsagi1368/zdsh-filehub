# Host-provided packages must never be fetched from any registry: they are
# either declared as optional peers (resolved by the host at runtime) or
# linked locally for development.
auto-install-peers=false
